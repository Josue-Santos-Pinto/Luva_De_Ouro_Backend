{
    "version": 2,
    "builds": [
        {
            "src": "./.env",
            "use":"@vercel/node"
        }
    ],
    "routes": [
        {
            "src":"/(.*)",
            "dest": "/"
        }
    ]
}